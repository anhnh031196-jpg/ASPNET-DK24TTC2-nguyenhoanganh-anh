@model WebsiteDatLichChuyenNha.Models.PricingSetting

@{
    ViewData["Title"] = "Quản Lý Bảng Giá";
}

<div class="fade-in">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>💰 Quản Lý Bảng Giá</h2>
        <a asp-action="Index" class="btn btn-outline-primary">← Về Dashboard</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">⚙️ Cài Đặt Giá</h5>
                </div>
                <div class="card-body">
                    <form asp-action="UpdatePricing" method="post">
                        <input type="hidden" asp-for="Id" />

                        <div class="mb-4">
                            <label asp-for="PricePerKm" class="form-label"></label>
                            <div class="input-group">
                                <input asp-for="PricePerKm" class="form-control" type="number" step="1000" required />
                                <span class="input-group-text">VNĐ/km</span>
                            </div>
                            <span asp-validation-for="PricePerKm" class="text-danger"></span>
                            <small class="text-muted">Giá mỗi km sẽ được nhân với khoảng cách để tính chi phí vận chuyển</small>
                        </div>

                        <div class="mb-4">
                            <label asp-for="BaseServiceFee" class="form-label"></label>
                            <div class="input-group">
                                <input asp-for="BaseServiceFee" class="form-control" type="number" step="10000" required />
                                <span class="input-group-text">VNĐ</span>
                            </div>
                            <span asp-validation-for="BaseServiceFee" class="text-danger"></span>
                            <small class="text-muted">Phí dịch vụ cố định cho mỗi đơn hàng</small>
                        </div>

                        <div class="alert alert-info">
                            <strong>📊 Công Thức Tính Giá:</strong>
                            <p class="mb-0">Tổng Chi Phí = (Khoảng Cách × Giá/km) + Phí Dịch Vụ Cố Định</p>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">💾 Lưu Cài Đặt</button>
                        </div>
                    </form>
                </div>
            </div>

            @if (Model.UpdatedAt != DateTime.MinValue)
            {
                <div class="text-center mt-3 text-muted">
                    <small>Cập nhật lần cuối: @Model.UpdatedAt.ToString("dd/MM/yyyy HH:mm")</small>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
